import 'package:flutter/material.dart';

class MensualidadPage extends StatelessWidget {
  const MensualidadPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Mensualidad Page'),),
    );
  }
}