import 'package:flutter/material.dart';

class HandicapPage extends StatelessWidget {
  const HandicapPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Handicap Page'),),
    );
  }
}